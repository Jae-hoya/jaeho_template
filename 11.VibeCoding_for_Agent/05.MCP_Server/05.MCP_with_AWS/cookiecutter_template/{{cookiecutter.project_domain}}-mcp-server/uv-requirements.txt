# This file is used by the Dockerfile to install dependencies
# It should contain the same dependencies as pyproject.toml
# Generate this file with: uv pip compile pyproject.toml -o uv-requirements.txt

loguru>=0.7.3
mcp[cli]>=1.3.2
pydantic>=2.10.6
