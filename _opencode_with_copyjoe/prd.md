@copyjoe_prd_v1.0.md 를 보고 작성해줘. 랜딩페이지도 확인할 수 있어야하고, langchain단에서 구현된것들이 품질검사도 할 수 있게 jupyter도 있으면 좋겠어.
나는 fastapi에 대해서 지식이 부족하므로, fastapi로도 어떻게 확인할 수 있는지 잘 설명해줘

잘 돌아가는지도 확인해 줘야하며, 
langchain구현할때는 docs-langchain mcp을, 그외에 필요헤 따라 context7, platwright mcp를 사용해도 좋아.

LangChain 구현 시에는 docs-langchain MCP를 사용해도 좋아. 공식 문서 기반 코드 패턴으로 체인을 구성하고, deprecated API 사용을 방지해야해.

프로젝트 구조 파악 및 코드 정합성 검증 시에는 context7 MCP를 사용해 기존 코드와의 일관성을 유지해야해.

랜딩페이지 분석이 필요한 경우에는  Tavily로 URL을 찾은 뒤 Playwright MCP로 실제 DOM을 가져와.
본문, h1/h2, CTA 버튼 텍스트를 추출할 해야해.

LangChain, langgraph 체인의 품질 검사는 Jupyter 환경에서 수행하고,
RAG 검색 결과, structured output, 생성 품질을 검증할 해야해.

FastAPI 서버는 /docs, /openapi.json, /health를 통해 정상 동작 여부를 반드시 확인할 것.

실제 서비스단에서는 ollama를 통해 qwen 30b짜리를 쓸거긴한데, 일단은 openai로 여기서 체크를 할꺼야.

----------------------------------------------------
랜딩 페이지는 vue로 해달라니까 왜 vue  파일 형식이 없어?
그리고 파일 코드 형식이 좀 복잡한 느낌도 들어. 

랭그래프, 랭체인이 직관적으로 어디서 실행되는지도 모르겠고. 
지금 기능들을 싹다 보아서, 대화형 시스템 자체로 예시를 들어줬으면 좋겠어. 이게 py파일 ipynb파일(테스트)로 해서 대화형으로 만들길 원해. 

--------------------------------------------------
pain_point, defferentitator, objective, channel, langauge에 대해서 좀 자세히 설명할 필요가 있다는 생각이들었어. 
나도 한번에 이해하기 어려웠거든. 그리고 language는 어떤것들을 지원하는지 어떤것들이 어떤식으로 기재하는지도 적어주면 좋을거같아


--------------------------------------------------
내 생각에는 language도 클릭할 수 있으면 좋겠고, 예시 뿐만아니라 설명이 있음좋겠어. pain_point라던지, diffrentitor이라던지 말야. 그리고 사용자들은 top_k라고 하면 이것이 무슨뜻인지 모를 가능성도 크지. 차라리 대화를 하면서, 이런것들을 네가 이끌어 내는 방식이 좋은거같아. 그러니까 프롬프트와, copy style만 작게 입력하게 하는거지!


--------------------------------------------------------

처리 중에는 카피생성을 하지 못하게 해야해. 그리고 간편대화형은  클릭이 아니라, 모두다 대화형으로 진행했으면 좋겠어. 그리고 말한게, 상세입력에서 pain_point나 differentiator 정의를 제대로 해줬으면 좋겠어. 예시 표현은, 좀더 마케팅에 맞는 예시가 들어가야해.

------------------------- 

이 생성된 것들을 대화 기반으로 계속 나아지게 만드는게 필요해.
그리고 프롬프트 섹션은 사이드바로 넣어야하고 메인은 제너레이션 섹션이 되야해.

------------------------------
generation section 부분에도 초기화 부분이 있어야 할거같아.

모든 피드백 반영 재생성에서는, 글을 풍부하게 작성해줘야해

그리고 간편대화형 예시에 slogann, sns, description, storyboard_outline, rationale이 없어

상세입력 부분에는, 
differentiator, pain_point, product_name, objective, 클릭 유도 (CTR 중심),channel, tone, language, top_k등이 있는데, ?표시를 놓고, 올려놓으면 설명을 할 수 있는 문구를 볼 수 있게 하는게 어떨까? 그리고, differentiator 부분에서 예시는 좀더 마케팅에 맞게 해야지, 아니면 아에 빼던가.
그리고 랜딩분석 이게 올바른지 모르겠어. 이 랜딩으로 사용자들이 뭘 알수 있을까?

------------------------------------------
골든 데이터셋을 위해서, 질문과 예상답변에 대해서 만들어줄래?
음 내가 원하는건, 실제 마케터들이 할만한 질문과, 그에맞는 카피 아이디어가 나와야 해

-----------------------------------------
지금 대화약입력할때마다 예시들이 있는데, 이것들을 좀 바꿔야 할 필요가 있을거같아.
아까말한 실무에맞는 질문들이 필요해
그리고, 현재 창이 양옆으로 좀 남는거 같아. 조금 넓게볼수 있게 해줘.
간퍈 대화형에서, 스크롤이 자동으로 내려 가게 해줘

-------------------------------------------------
간편 대화형에서, 언어는 따로 밑에 클리갛게 하고, 약간의 오타가 있어도 알아서 판단하게 해줬으면 좋겠어. 오류가 났다면 어디서 못알아 들어서 오류가 났는지 알려줬으면 좋겠어(422 오류가 발생했었음)

----------------------------------------------
- 입력
입력 텍스트 프롬프트 자연어 최대8000자 자유형 질문/요청
업로드 할 수 있는 개인데이터로는 pdf, doc, docx, txt, xls, xlsx, ppt, pptx, png, jpg
웹 검색 키워드: 검색 쿼리, 실시간 정보 수집을 위한 검색어

- 출력
멀티 공유 가능한응답. json/md, doc/docx파일
사용이력: 쓰레드 형식의 개인 프로픔트 및 결과 관리

- 주요기능
프롬프트에 대한 최적 응답 제공



--------------------------------- 
config - 모델변경
copy_service - prompt 변경